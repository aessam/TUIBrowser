// TUICSSParser - CSS tokenizer, parser, and rules
// This module provides CSS parsing capabilities including:
// - Tokenization (CSSTokenizer, CSSToken)
// - Selectors (Selector, SimpleSelector, Combinator)
// - Specificity calculation
// - Values (CSSValue, LengthUnit)
// - Declarations (CSSDeclaration)
// - Rules and Stylesheets (CSSRule, Stylesheet)
// - High-level parsing API (CSSParser)

import TUICore

/// CSS Parser module namespace
public enum TUICSSParser {
    public static let version = "0.1.0"
}
